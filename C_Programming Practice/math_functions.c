#include <stdio.h>
#include <math.h>

int main(){

    int x = 2;
    int y = 4;

    x = sqrt(x);
    y = pow(y,3);

    printf('%d',x);
    printf('%d',y);
    //round
    //ceil




    return 0;
}
